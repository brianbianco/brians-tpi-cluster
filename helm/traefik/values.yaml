deployment:
  enabled: true
  kind: DaemonSet

service:
  annotations:
    external-dns.alpha.kubernetes.io/hostname: "traefik.brians.computer"

api:
  insecure: true
  dashboard: true

logs:
  access:
    enabled: true

ports:
  ssh:
    port: 2222
    exposedPort: 22
    expose:
      default: true
    protocol: TCP
