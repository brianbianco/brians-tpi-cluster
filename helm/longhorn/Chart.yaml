apiVersion: v2
name: longhorn-install
description: A wrapper chart for Longhorn with custom values.
version: 0.1.0
dependencies:
  - name: longhorn
    version: "1.11.2"
    repository: "https://charts.longhorn.io"
